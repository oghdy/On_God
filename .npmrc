# Expo/Metro는 @expo/cli·metro·@babel/runtime 등 깊은 transitive 의존성을 프로젝트
# node_modules에서 직접 require()한다. pnpm 기본(isolated) 레이아웃은 이런 접근을 막으므로
# (phantom dependency 방지 목적), Expo 공식 pnpm 모노레포 가이드대로 hoisted 링커를 쓴다.
# 관련: docs/decisions/0005-pnpm-hoisted-linker.md
node-linker=hoisted
