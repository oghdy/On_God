# 이 파일을 복사해서 .env로 만들고 실제 값을 채운다 (루트 .env의 SUPABASE_DEV_* 값을 그대로 옮기면 됨).
# EXPO_PUBLIC_ 접두사가 붙어야 Expo가 클라이언트 번들에 노출한다 — service_role 키는 절대 여기 두지 않는다.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# P2-S7-T3: 크래시 리포팅(선택). 없으면 Sentry 초기화를 그냥 건너뛴다.
EXPO_PUBLIC_SENTRY_DSN=
