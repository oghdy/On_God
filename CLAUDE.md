# OnGod — 프로젝트 작업 규칙

이 파일은 **이 프로젝트(OnGod) 폴더에서만** 적용되는 규칙이다. 다른 프로젝트에는 영향을 주지 않는다.
Claude Code(또는 호환 에이전트)가 이 폴더에서 세션을 시작하면 자동으로 읽는다.

프로젝트 소유자는 개발 지식이 아직 많지 않은 학생이다. 코드/설정에 관한 결정은 임의로
진행하되, 사람만 할 수 있는 작업(🧑)은 반드시 명확히 요청하고 대신 하려 하지 않는다.

## 세션 시작 시 항상 이 순서로 읽는다

1. `docs/OVERVIEW.md` — 전체 흐름, 마일스톤, 설계 대원칙
2. `docs/logs/handoff.md` — 다른 트랙이 나에게 남긴 항목이 있는지 확인. 미해결(`[ ]`) 항목 중
   내 트랙 것이 있으면 먼저 처리하거나, 처리 계획을 세운다.
3. 작업할 `docs/phase-N-*.md` — 해당 Phase의 Step/Task 목록에서 다음 미완료 Task 확인
4. 필요 시 관련 `docs/decisions/*.md` (ADR)

이 프로젝트는 아직 git 저장소가 아니다(`git init` 안 됨). 커밋 관련 작업 전에 저장소
초기화 여부를 확인하고, 없으면 사람에게 GitHub 레포 생성 여부를 먼저 확인한다.

## 담당자 구분

- 🤖 **에이전트가 직접 수행**한다.
- 🧑 **사람만 할 수 있다** — 외부 서비스 계정 생성, API 키 발급, 결제, 스토어 제출, 실기기
  테스트 등. `docs/human-actions.md`에 이미 정리되어 있는지 확인하고, 필요한 값을 명확하게
  요청한다 (무엇을, 어디서, 왜 필요한지).
- 🤝 **협업** — 에이전트가 코드/설정을 준비하고, 사람이 인증·키 입력·승인만 수행한다.

🧑·🤝 항목을 절대 대신 처리하려 하지 않는다 (계정 생성 시도, 키 추측, 결제 진행 금지).

## Task 작업 루프

1. `docs/phase-N-*.md`에서 다음 미완료 Task를 고른다 (또는 `handoff.md`의 내 트랙 항목).
2. 구현한다.
3. 커밋 메시지에 Task ID를 포함한다. 예: `P1-S2-T1: Apple Music 어댑터 구현`
4. `docs/logs/{backend|frontend}-log.md`에 템플릿대로 작업 기록을 append한다 (파일 안의
   템플릿 참고). 과거 항목은 절대 수정·삭제하지 않는다.
5. `docs/phase-N-*.md`의 해당 체크박스를 `[x]`로 바꾸고, 방금 쓴 로그 항목을 링크한다.
6. 이 변경이 다른 트랙(프론트↔백엔드)에 영향을 준다면 `docs/logs/handoff.md`에 항목을
   추가한다. 빠뜨리면 다른 세션이 모르고 지나간다 — 판단이 애매하면 일단 남긴다.
7. 되돌리기 어려운 구조적 결정을 내렸다면 `docs/decisions/000N-짧은-제목.md`로 ADR을
   작성한다 (아래 결정 권한 표 참고).

## 결정 권한 3단계

작업 중 계획에 없던 것을 스스로 정해야 할 때, 아래 기준으로 판단한다.

| 단계 | 예시 | 처리 방법 |
|------|------|-----------|
| **전술적 Task** | 세부 헬퍼 함수, 계획에 없던 작은 구현 단위 | 그 자리에서 Task ID(`P#-S#-T#b` 등)를 붙여 `phase-N-*.md`에 추가하고 진행. 저장소 소유자에게 물어볼 필요 없음. |
| **횡단적 영향** | 내 변경이 다른 트랙의 코드/화면/데이터에 영향 | 진행하되 `handoff.md`에 필수 기록. |
| **구조적 결정** | 되돌리기 어려운 선택 (DB 스키마 근본구조, 인증 방식, 핵심 라이브러리 채택 등) | ADR 작성 필수. 영향이 크면 진행 전에 사람에게 확인. |

## 로그 작성 템플릿 (요약)

전체 템플릿은 `docs/logs/backend-log.md` / `frontend-log.md` 안에 있다.

```markdown
## YYYY-MM-DD · P#-S#-T# — 한 줄 제목

**Task**: [P#-S#-T#](../phase-N-*.md#관련-섹션)
**한 일**: 무엇을 구현/변경했는지
**왜 이렇게**: 선택한 방식과 이유
**변경 파일**: 경로 목록
**검증**: 어떻게 확인했는지
**막힌 점 / 다음 할 일**: 있으면 기록
```

## 아키텍처 원칙 (모든 코드에 적용)

`docs/OVERVIEW.md`의 설계 대원칙 요약:

1. 경계를 명확히 한다 (모노레포 패키지 분리, `apps`는 `packages`를 쓰고 역은 금지)
2. 외부 의존성은 어댑터 뒤에 숨긴다 (`packages/integrations`)
3. 타입을 공유한다 (DB 스키마 → 자동 생성 타입, 도메인 타입 매핑)
4. 데이터를 먼저 만든다 (Phase 1 파이프라인이 Phase 2 앱보다 우선)
5. 부수효과는 경계 레이어로 민다 (비즈니스 로직은 순수 함수)
6. 설정은 코드 밖으로 (시크릿은 env, 운영 파라미터는 DB)

Service Role 키, 외부 API 키 등 시크릿은 절대 클라이언트 코드/커밋에 포함하지 않는다.
